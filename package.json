{
  "name": "zenith-umbra",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "serve:local": "node serve-local.mjs 4848",
    "anim:fixtures": "vitest run -c tests/tools/vitest.config.ts tests/tools/animpack.test.ts"
  },
  "dependencies": {
    "mqtt": "^5.16.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/three": "^0.186.0",
    "puppeteer-core": "^25.12.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
